# secrets — never commit
.env
.env.*
secrets/
*.pem
!site/.well-known/appspecific/com.tesla.3p.public-key.pem
# local-only (contains home layout)
mockups/d-rooftop.html
# tooling
node_modules/
.vercel
.DS_Store

# personal data (bills, readings, tokens) — local only
data/
web/dist/
.vercel
!web/public/.well-known/appspecific/com.tesla.3p.public-key.pem
